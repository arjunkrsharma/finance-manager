apiVersion: v1
kind: Service
metadata:
  name: {{ include "finance-chart.fullname" . }}
  labels:
    {{- include "finance-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finance-chart.selectorLabels" . | nindent 4 }}
